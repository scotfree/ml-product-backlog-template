# Replace stubs with real components (incremental)

_Swap one stub at a time. The pipeline stays runnable at every step._

- **Upstream/downstream:** _Which stage do you own, and which teammate's stage feeds yours?_
- **Definition of done:** _Real component passes the same interface tests as the stub it replaced._
- **Demo:** _Show the pipeline running with the new real stage; contrast output with the stub version._
- **Subtasks:**
  - _One card per stub-to-real swap._
